<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <dimen name="ButtonSize">96dp</dimen>
    <dimen name="ButtonPadding">16dp</dimen>
    <style name="MainButtonStyle">
        <item name="android:layout_width">@dimen/ButtonSize</item>
        <item name="android:layout_height">@dimen/ButtonSize</item>
        <item name="android:padding">@dimen/ButtonPadding</item>
    </style>

    <style name="MainButtonTextStyle">
        <item name="android:layout_width">@dimen/ButtonSize</item>
        <item name="android:textAlignment">center</item>

    </style>

    <dimen name="AddButtonSize">56dp</dimen>
    <color name="AddButtonBGColor">#32ba94</color>
    <style name="AddButtonStyle">
        <item name="android:layout_width">@dimen/AddButtonSize</item>
        <item name="android:layout_height">@dimen/AddButtonSize</item>
        <item name="android:background">@drawable/add_button</item>
    </style>

    <dimen name="ReciepeListItemHeight">110dp</dimen>

</resources>
